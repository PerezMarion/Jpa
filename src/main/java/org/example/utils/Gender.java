package org.example.utils;

public enum Gender {
    MALE, // Première valeur dans l'enum donc valeur associée 0
    FEMALE, // Deuxième valeur dans l'enum donc valeur associée 1
    OTHER; // Troisième valeur dans l'enum donc valeur associée 2
}
